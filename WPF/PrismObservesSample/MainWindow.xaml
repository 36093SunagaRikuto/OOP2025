<Window x:Class="PrismObservesSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrismObservesSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="100" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition />
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Grid.Column="0" Height="30" Margin="5" Text="{Binding input1}" TextChanged="TextBox_TextChanged"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Height="30" Margin="5"
                   Text="+" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <TextBox Grid.Row="0" Grid.Column="2" Height="30" Margin="5" Text="{Binding input2}"/>
        <TextBlock Grid.Row="0" Grid.Column="3" Height="30" Margin="5"
                   Text="=" TextAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <TextBlock Grid.Row="0" Grid.Column="4" Height="30" Margin="5" Text="{Binding result}"/>
        <Button Grid.Row="1" Grid.Column="4" Content="計算" Height="50" Command="{Binding Sumcommand}"/>

    </Grid>
</Window>
